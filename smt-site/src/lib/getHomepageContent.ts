import { homepageContent } from "@/content/homepage";

export async function getHomepageContent() {
  // Replace with headless CMS fetch (e.g. Webflow/WordPress/Contentful) when ready.
  return homepageContent;
}
